import capitalize from 'tocaptcase';

const textoDeTeste = 'hello, npm!, JAIRO | hoje é UM Ótimo dia para PROGRAMAR';

const resultado = capitalize(textoDeTeste);

console.log(`Entrada: ${textoDeTeste}`);
console.log(`Saída: ${resultado}`);